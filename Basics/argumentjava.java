
public class argumentjava {

    public static void main(String[ ] args){
        
        System.out .print(args[0]+" " +args[1]);//We have to take the input while the code is running 
        //This will be taken as a string array so any input is taken will be as string 
        int n=Integer.parseInt(args[0]);
    }
}
